# 📋 قائمة جميع الأوامر المستخدمة في المشروع

## 🎯 فهرس الأوامر

1. [أوامر تثبيت الأدوات](#أوامر-تثبيت-الأدوات)
2. [أوامر Flutter الأساسية](#أوامر-flutter-الأساسية)
3. [أوامر Firebase](#أوامر-firebase)
4. [أوامر npm/Node](#أوامر-npmnode)
5. [أوامر التطوير والاختبار](#أوامر-التطوير-والاختبار)
6. [أوامر التنظيف والإصلاح](#أوامر-التنظيف-والإصلاح)

---

# 🔧 الأوامر المفصلة

## أوامر تثبيت الأدوات

### 1️⃣ تثبيت FlutterFire CLI
```bash
dart pub global activate flutterfire_cli
```
**الفائدة:** 
- تثبيت أداة FlutterFire التي تساعد في ربط Firebase مع Flutter تلقائياً
- بدون هذا الأمر، لا يمكن استخدام `flutterfire configure`

**الوقت:** ~30 ثانية

---

### 2️⃣ تثبيت Firebase CLI
```bash
npm install -g firebase-tools
```
**الفائدة:**
- تثبيت أدوات Firebase بشكل عام في النظام
- مطلوب لـ FlutterFire CLI للعمل
- يسمح بـ `firebase login` و `firebase deploy`

**المتطلبات:** Node.js و npm يجب أن يكونوا مثبتين

**الوقت:** ~1 دقيقة

---

### 3️⃣ التحقق من إصدارات Node و npm
```bash
node --version && npm --version
```
**الفائدة:**
- تحقق من وجود Node.js و npm على الجهاز
- إذا لم تظهر النسخ، يعني يجب تثبيت Node.js

**مثال على الناتج:**
```
v22.20.0
11.4.2
```

---

## أوامر Flutter الأساسية

### 4️⃣ تحميل جميع الـ Dependencies
```bash
flutter pub get
```
**الفائدة:**
- تحميل جميع الـ packages المطلوبة من pubspec.yaml
- ضروري بعد:
  - إنشاء مشروع جديد
  - تعديل pubspec.yaml
  - حذف folder pubspec.lock

**الوقت:** 30 ثانية - 2 دقيقة

---

### 5️⃣ تنظيف المشروع
```bash
flutter clean
```
**الفائدة:**
- حذف جميع الملفات المُترجمة والمؤقتة
- إصلاح المشاكل الغريبة والأخطاء الغريبة
- مثل "restart the IDE"

**الوقت:** 10 ثواني

**متى تستخدمه:**
- عند حدوث أخطاء غريبة
- قبل تشغيل التطبيق لأول مرة
- عند تغيير الـ packages

---

### 6️⃣ تشغيل التطبيق
```bash
flutter run
```
**الفائدة:**
- تشغيل التطبيق على Emulator أو جهاز حقيقي
- البناء والتجميع والتشغيل تلقائياً

**خيارات مفيدة:**
```bash
flutter run -v              # Verbose mode (معلومات مفصلة)
flutter run -d emulator-5554  # تشغيل على جهاز محدد
flutter run --release       # تشغيل release mode
```

**الوقت:** 2-5 دقائق (المرة الأولى)

---

### 7️⃣ بناء التطبيق بدون تشغيل
```bash
flutter build apk
```
**الفائدة:**
- بناء ملف APK للتوزيع على Google Play
- لا يتم التشغيل، فقط البناء

**خيارات أخرى:**
```bash
flutter build ios         # بناء iOS
flutter build web         # بناء Web
flutter build windows     # بناء Windows
```

---

### 8️⃣ التحقق من الأخطاء في الكود
```bash
flutter analyze
```
**الفائدة:**
- فحص الكود للأخطاء والتحذيرات
- التحقق من جودة الكود
- لا يقوم بـ build، فقط تحليل

**الناتج:**
- `error` - أخطاء يجب إصلاحها
- `warning` - تحذيرات يفضل إصلاحها
- `info` - معلومات فقط

---

### 9️⃣ اختبار التطبيق
```bash
flutter test
```
**الفائدة:**
- تشغيل جميع الـ unit tests و widget tests
- التحقق من صحة الكود

**الوقت:** يعتمد على عدد الـ tests

---

### 🔟 قائمة الأجهزة المتاحة
```bash
flutter devices
```
**الفائدة:**
- عرض جميع الأجهزة والـ Emulators المتاحة
- اختيار الجهاز المراد التشغيل عليه

**مثال على الناتج:**
```
2 connected devices:

Android SDK built for x86 (mobile) • emulator-5554 • android-x86    • Android 13 (API 33)
Chrome (web)                       • chrome       • web-javascript • Chrome 120.0
```

---

## أوامر Firebase

### 1️⃣ تسجيل الدخول لـ Firebase
```bash
firebase login
```
**الفائدة:**
- تسجيل الدخول لحسابك في Google
- مطلوب لـ FlutterFire CLI

**ملاحظة:** قد يحتاج لـ `--no-localhost` إذا كنت في بيئة بدون browser

---

### 2️⃣ إعداد Firebase للمشروع
```bash
flutterfire configure
```
**الفائدة:**
- ربط المشروع مع Firebase تلقائياً
- يسأل أسئلة ويملأ البيانات
- ينشئ `firebase_options.dart` تلقائياً

**الخطوات التي تحصل:**
1. اختيار Platforms (Android/iOS/Web)
2. اختيار Firebase Project
3. تكوين كل Platform
4. إنشاء firebase_options.dart

**الوقت:** 2-3 دقائق

---

### 3️⃣ عرض إصدار Firebase CLI
```bash
firebase --version
```
**الفائدة:**
- معرفة إصدار Firebase CLI المثبت
- للتأكد من أنه مثبت بشكل صحيح

---

### 4️⃣ نشر المشروع على Firebase
```bash
firebase deploy
```
**الفائدة:**
- نشر الـ Cloud Functions و Firestore Rules
- نشر Hosting (إذا كان هناك web)

**ملاحظة:** ليس مطلوباً لـ Flutter app العادي

---

## أوامر npm/Node

### 1️⃣ التحقق من إصدار npm
```bash
npm --version
```
**الفائدة:**
- معرفة إصدار npm المثبت
- التأكد من التثبيت الصحيح

---

### 2️⃣ تحديث npm
```bash
npm install -g npm@latest
```
**الفائدة:**
- تحديث npm للنسخة الأحدث
- للحصول على أحدث الميزات

---

### 3️⃣ تثبيت package عام
```bash
npm install -g package-name
```
**الفائدة:**
- تثبيت أي package عالمياً على النظام
- الـ `-g` = global

---

## أوامر التطوير والاختبار

### 1️⃣ مشاهدة الأخطاء والتحذيرات
```bash
flutter analyze --fatal-infos
```
**الفائدة:**
- فحص الكود بشكل صارم
- يعامل التحذيرات كأخطاء

---

### 2️⃣ عرض معلومات النظام
```bash
flutter doctor
```
**الفائدة:**
- فحص شامل لـ Flutter installation
- التحقق من:
  - Flutter SDK
  - Dart SDK
  - Java/Android SDK
  - Xcode (iOS)
  - IDE setup
  - Devices

**الناتج:** 
```
✓ = مثبت وصحيح
✗ = غير مثبت أو خطأ
```

---

### 3️⃣ فحص التبعيات المتقادمة
```bash
flutter pub outdated
```
**الفائدة:**
- عرض الـ packages التي فيها نسخ أحدث
- تجنب الأمان والأخطاء

---

## أوامر التنظيف والإصلاح

### 1️⃣ حذف جميع الملفات المؤقتة
```bash
flutter clean
```
**الفائدة:**
- حذف build/ و .dart_tool/ و pubspec.lock
- إعادة تشغيل نظيفة

---

### 2️⃣ تحديث الـ packages
```bash
flutter pub upgrade
```
**الفائدة:**
- تحديث جميع packages لأحدث نسخة
- حل مشاكل التبعيات

---

### 3️⃣ إصلاح المشاكل تلقائياً
```bash
dart fix --apply
```
**الفائدة:**
- إصلاح تلقائي للأخطاء البسيطة
- تطبيق الإصلاحات مباشرة

---

### 4️⃣ تنسيق الكود
```bash
dart format lib/
```
**الفائدة:**
- تنسيق الكود بحسب معايير Dart
- جعل الكود منسقاً بشكل موحد

---

# 🚀 سيناريوهات الاستخدام

## السيناريو 1️⃣: إعداد مشروع جديد

```bash
# 1. إنشاء مشروع (يعمل تلقائياً عند الإنشاء)
flutter create my_app

# 2. الدخول للمشروع
cd my_app

# 3. تحميل الـ dependencies
flutter pub get

# 4. التحقق من الإعداد
flutter doctor

# 5. تشغيل التطبيق
flutter run
```

---

## السيناريو 2️⃣: إضافة Firebase

```bash
# 1. تثبيت FlutterFire
dart pub global activate flutterfire_cli

# 2. إعداد Firebase
flutterfire configure

# 3. تحديث الـ dependencies
flutter pub get

# 4. تشغيل التطبيق
flutter run
```

---

## السيناريو 3️⃣: حل مشكلة غريبة

```bash
# 1. تنظيف المشروع
flutter clean

# 2. حذف Dart Tool
rmdir /s .dart_tool

# 3. تحميل الـ dependencies
flutter pub get

# 4. تحليل الأخطاء
flutter analyze

# 5. تشغيل مجدداً
flutter run
```

---

## السيناريو 4️⃣: نشر التطبيق

```bash
# 1. بناء APK (Android)
flutter build apk --release

# 2. بناء IPA (iOS)
flutter build ios --release

# 3. الملفات جاهزة في:
# Android: build/app/outputs/flutter-apk/app-release.apk
# iOS: build/ios/ipa/Runner.ipa
```

---

# 📋 جدول سريع للأوامر

| الأمر | الفائدة | الوقت |
|------|--------|------|
| `flutter pub get` | تحميل الـ packages | 30 ثانية |
| `flutter clean` | تنظيف المشروع | 10 ثواني |
| `flutter run` | تشغيل التطبيق | 2-5 دقائق |
| `flutter analyze` | فحص الأخطاء | 10 ثواني |
| `flutter test` | اختبار التطبيق | متغير |
| `flutter doctor` | فحص الإعداد | 30 ثانية |
| `flutterfire configure` | إعداد Firebase | 2-3 دقائق |
| `firebase login` | تسجيل الدخول | 1 دقيقة |
| `dart format` | تنسيق الكود | 5 ثواني |

---

# 🎯 أوامر يومية

### قبل البدء بالعمل:
```bash
flutter pub get        # تحديث الـ packages
flutter analyze        # فحص الأخطاء
```

### أثناء التطوير:
```bash
flutter run            # تشغيل التطبيق
dart format lib/       # تنسيق الكود
```

### عند الانتهاء:
```bash
flutter test           # اختبار التطبيق
flutter build apk      # بناء APK
```

---

# ⚠️ أخطاء شائعة والحل

| الخطأ | الحل |
|-------|------|
| `error: Target of URI doesn't exist` | `flutter pub get` |
| `gradle build failed` | `flutter clean && flutter pub get` |
| `flutterfire command not found` | `dart pub global activate flutterfire_cli` |
| `firebase not found` | `npm install -g firebase-tools` |
| `Permission denied` | استخدم `sudo` أو أضف للـ PATH |

---

# 💾 حفظ الأوامر

### في Windows (PowerShell):
```powershell
# إنشاء alias
Set-Alias -Name fc -Value "flutter clean"
Set-Alias -Name fpg -Value "flutter pub get"
Set-Alias -Name fr -Value "flutter run"
```

### في Linux/Mac (Bash):
```bash
# أضف في ~/.bashrc أو ~/.zshrc
alias fc="flutter clean"
alias fpg="flutter pub get"
alias fr="flutter run"
```

---

**حفظ هذا الملف للمراجعة السريعة! 📌**
