# 📸 شرح مرئي للأوامر

## 1️⃣ تثبيت FlutterFire

```
┌─────────────────────────────────────────────┐
│ dart pub global activate flutterfire_cli    │
└─────────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────────────┐
│ Downloading packages...                      │
│ Building package executables...             │
│ Built flutterfire_cli:flutterfire.          │
│ Activated flutterfire_cli 1.3.1. ✓         │
└─────────────────────────────────────────────┘
```

---

## 2️⃣ تحميل الـ Dependencies

```
┌─────────────────────────────────────────────┐
│ flutter pub get                             │
└─────────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────────────┐
│ Resolving dependencies...                    │
│ Downloading packages...                      │
│ • firebase_core                             │
│ • firebase_auth                             │
│ • google_sign_in                            │
│ • flutter_facebook_auth                     │
│ • flutter_bloc                              │
│ • equatable                                 │
│ Got dependencies! ✓                         │
└─────────────────────────────────────────────┘
```

---

## 3️⃣ فحص الأخطاء

```
┌─────────────────────────────────────────────┐
│ flutter analyze                             │
└─────────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────────────┐
│ Analyzing to_do_app_with_bloc...            │
│                                             │
│ ✓ No errors found                          │
│ ⓘ 5 info warnings (غير مهمة)              │
│                                             │
│ Analysis successful! ✓                      │
└─────────────────────────────────────────────┘
```

---

## 4️⃣ تشغيل التطبيق

```
┌─────────────────────────────────────────────┐
│ flutter run                                 │
└─────────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────────────┐
│ Building for Android (first build)...       │
│ • Compiling Kotlin files                    │
│ • Building APK                              │
│ • Installing APK                            │
│                                             │
│ • App launched successfully! ✓              │
│                                             │
│ 🔥 Hot Reload Ready                        │
│ 🔥 Hot Restart Ready                       │
└─────────────────────────────────────────────┘
```

---

## 5️⃣ فحص الإعداد

```
┌─────────────────────────────────────────────┐
│ flutter doctor                              │
└─────────────────────────────────────────────┘
            ↓
┌─────────────────────────────────────────────┐
│ Flutter (Channel stable, 3.x.x)             │
│ ✓ Framework • revision xxx                  │
│                                             │
│ Android toolchain                           │
│ ✓ Android SDK at xxx                        │
│                                             │
│ Xcode (for iOS)                             │
│ ✓ Xcode 15.0 installed                     │
│                                             │
│ Android Studio                              │
│ ✓ Android Studio xxx installed             │
│                                             │
│ • No issues found! ✓                        │
└─────────────────────────────────────────────┘
```

---

## 🔄 سيناريو كامل

### الإعداد الأول:

```
START
  ↓
dart pub global activate flutterfire_cli
  ↓ (✓ تم)
npm install -g firebase-tools
  ↓ (✓ تم)
flutter pub get
  ↓ (✓ تم)
flutter analyze
  ↓ (✓ تم)
flutter run
  ↓ (✓ التطبيق يشتغل!)
DONE ✅
```

---

### عند كل تعديل:

```
EDIT CODE
  ↓
flutter analyze (فحص سريع)
  ↓
flutter run (تشغيل)
  ↓ (Hot Reload)
رؤية التغييرات فوراً ✨
```

---

### عند حدوث مشكلة:

```
❌ ERROR OCCURRED
  ↓
flutter clean (تنظيف)
  ↓
flutter pub get (تحديث)
  ↓
flutter analyze (فحص)
  ↓
flutter run (محاولة جديدة)
  ↓
✅ إذا نجح → انتهى المشكلة
❌ إذا فشل → استخدم flutter run -v
```

---

## 📊 مخطط الأوامر الأساسية

```
┌─────────────────────────────────────────────┐
│          PROJECT WORKFLOW DIAGRAM           │
└─────────────────────────────────────────────┘

DEVELOPMENT PHASE:
┌─────────────┐
│ Edit Code   │
└──────┬──────┘
       ↓
┌─────────────────────────────┐
│ flutter pub get             │ (عند تغيير pubspec.yaml)
└──────┬──────────────────────┘
       ↓
┌─────────────────────────────┐
│ flutter analyze             │ (فحص الأخطاء)
└──────┬──────────────────────┘
       ↓
┌─────────────────────────────┐
│ flutter run                 │ (تشغيل)
└──────┬──────────────────────┘
       ↓
┌─────────────────────────────┐
│ Hot Reload / Hot Restart    │ (تطبيق التغييرات)
└──────┬──────────────────────┘
       ↓
    REPEAT...

BUILD PHASE:
┌─────────────────────────────┐
│ flutter clean               │ (تنظيف)
└──────┬──────────────────────┘
       ↓
┌─────────────────────────────┐
│ flutter build apk           │ (بناء APK)
└──────┬──────────────────────┘
       ↓
┌─────────────────────────────┐
│ build/app/outputs/...apk    │ (الملف النهائي)
└─────────────────────────────┘
```

---

## 🎯 أوقات التنفيذ:

```
dart pub global activate flutterfire_cli
└─ ⏱️ 30 ثانية

npm install -g firebase-tools
└─ ⏱️ 1 دقيقة

flutter pub get
└─ ⏱️ 30 ثانية - 2 دقيقة

flutter clean
└─ ⏱️ 10 ثواني

flutter analyze
└─ ⏱️ 10 ثواني

flutter run (أول مرة)
└─ ⏱️ 3-5 دقائق

flutter run (بعد التعديل)
└─ ⏱️ 10-30 ثانية

flutter build apk
└─ ⏱️ 5-10 دقائق
```

---

## 🆘 استكشاف الأخطاء

```
❌ "Target of URI doesn't exist"
   ↓
   FIX: flutter pub get

❌ "gradle build failed"
   ↓
   FIX: flutter clean && flutter pub get

❌ "flutterfire command not found"
   ↓
   FIX: dart pub global activate flutterfire_cli

❌ "firebase-tools not found"
   ↓
   FIX: npm install -g firebase-tools

❌ "Strange errors"
   ↓
   FIX: flutter clean && flutter pub get && flutter run -v
```

---

## 📋 نموذج إجابة سريعة:

عندما تقول "لا يشتغل"، جرّب بالترتيب:

```
1. flutter clean
2. flutter pub get
3. flutter analyze
4. flutter run -v
```

إذا استمرت المشكلة:

```
5. flutter doctor
6. أعد تشغيل IDE
7. أعد تشغيل الكمبيوتر
```

---

**احفظ هذا الملف كـ visual reference!** 📌
