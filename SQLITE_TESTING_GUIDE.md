# SQLite - دليل الاختبار السريع 🚀

## ✅ الحالة الحالية

- ✅ pubspec.yaml محدث مع SQLite
- ✅ DatabaseHelper جاهز
- ✅ UserDataSource مكتمل
- ✅ AuthRepositoryImpl مُصلح
- ✅ بدون أخطاء في المشروع

## 🔧 الخطوات التالية

### 1️⃣ تشغيل المشروع
```bash
flutter clean
flutter pub get
flutter run
```

### 2️⃣ اختبار التسجيل الجديد
- اضغط على "Sign Up" أو "إنشاء حساب"
- أدخل:
  - **الاسم:** محمد أحمد
  - **البريد:** user@example.com
  - **كلمة المرور:** 123456
- يجب أن تظهر dialog: "✓ تم التسجيل بنجاح"
- اضغط "اذهب للدخول"

### 3️⃣ اختبار تسجيل الدخول
- أدخل نفس البريد وكلمة المرور
- يجب أن تنجح عملية الدخول
- تأكد من الانتقال للـ Home Screen

### 4️⃣ اختبار متعدد (اختياري)
```dart
// إضافة في main.dart أو في method للاختبار
testSQLiteOperations();

Future<void> testSQLiteOperations() async {
  var repo = AuthRepositoryImpl();
  
  // اختبار 1: التسجيل
  try {
    var user1 = await repo.register(
      'ali@example.com',
      'pass123',
      'علي محمود'
    );
    print('✅ تسجيل: ${user1.email}');
  } catch (e) {
    print('❌ خطأ: $e');
  }
  
  // اختبار 2: تسجيل ثاني
  try {
    var user2 = await repo.register(
      'fatima@example.com',
      'pass456',
      'فاطمة أحمد'
    );
    print('✅ تسجيل: ${user2.email}');
  } catch (e) {
    print('❌ خطأ: $e');
  }
  
  // اختبار 3: عد المستخدمين
  int count = await repo.getUserCount();
  print('📊 عدد المستخدمين: $count');
  
  // اختبار 4: الحصول على جميع المستخدمين
  var allUsers = await repo.getAllUsers();
  print('👥 المستخدمون: ${allUsers.map((u) => u.email).toList()}');
  
  // اختبار 5: تسجيل الدخول
  try {
    var loggedIn = await repo.login('ali@example.com', 'pass123');
    print('✅ دخول: ${loggedIn.firstName} ${loggedIn.lastName}');
  } catch (e) {
    print('❌ فشل الدخول: $e');
  }
  
  // اختبار 6: المستخدم الحالي
  var current = repo.getCurrentUser();
  print('👤 المستخدم الحالي: ${current?.email}');
}
```

## 🐛 استكشاف الأخطاء

### المشكلة: عدم حفظ البيانات
**الحل:**
```dart
// تأكد من استدعاء SharedPreferences.getInstance()
// وتأكد من أن _initializePrefs() يُنفذ قبل الاستخدام
```

### المشكلة: البريد الإلكتروني مكرر
**الحل:**
- الجدول يحتوي على UNIQUE constraint على email
- سيظهر خطأ "البريد موجود"

### المشكلة: كلمة مرور غير صحيحة
**الحل:**
- تأكد من الكتابة الدقيقة (case-sensitive)
- لا توجد مسافات في البداية أو النهاية

## 📋 Checklist

- [ ] flutter clean تم تنفيذه
- [ ] flutter pub get تم تنفيذه
- [ ] لا توجد أخطاء (get_errors يرجع فارغ)
- [ ] التسجيل يعمل ويظهر Dialog
- [ ] تسجيل الدخول ينجح
- [ ] البيانات تُحفظ في SQLite
- [ ] SharedPreferences يحفظ المستخدم الحالي

## 🔗 الملفات المتعلقة

| الملف | النوع | الحالة |
|--------|--------|--------|
| pubspec.yaml | Config | ✅ |
| database_helper.dart | Source | ✅ |
| user_data_source.dart | Source | ✅ |
| user_model.dart | Model | ✅ |
| auth_repository_impl.dart | Repository | ✅ |
| register_screen.dart | UI | ✅ |
| login_screen.dart | UI | ✅ |
| register_bloc.dart | BLoC | ✅ |
| login_bloc.dart | BLoC | ✅ |

## 📞 معلومات إضافية

**مسار قاعدة البيانات:**
- Android/iOS: `/data/data/app_name/databases/users.db`

**حجم قاعدة البيانات:**
- صغير جداً (كل مستخدم ≈ 200-300 bytes)

**عدد المستخدمين الممكن:**
- غير محدود عملياً (SQLite يدعم ملايين الصفوف)

---

**هل تحتاج مساعدة إضافية؟ اسأل! 💬**
