# 📚 دليل الأوامر الشامل - كل ما تحتاج لمعرفته

## 🎯 ابدأ من هنا

اختر احتياجك:

- 📌 [الأوامر السريعة](#الأوامر-السريعة) - 5 أوامر فقط
- 📖 [الشرح الكامل](#الشرح-الكامل) - مع التفاصيل
- 🆘 [حل المشاكل](#حل-المشاكل) - عند حدوث خطأ
- 🚀 [البناء والنشر](#البناء-والنشر) - للنهاية

---

# الأوامر السريعة

## أهم 5 أوامر تحتاجها:

### 1️⃣ تحميل المكتبات
```bash
flutter pub get
```

### 2️⃣ تنظيف المشروع
```bash
flutter clean
```

### 3️⃣ تشغيل التطبيق
```bash
flutter run
```

### 4️⃣ فحص الأخطاء
```bash
flutter analyze
```

### 5️⃣ فحص الإعداد
```bash
flutter doctor
```

---

# الشرح الكامل

## الإعداد الأول (مرة واحدة فقط)

### الخطوة 1: تثبيت FlutterFire
```bash
dart pub global activate flutterfire_cli
```
**ماذا يفعل:** يثبت أداة ربط Firebase مع Flutter  
**متى:** عند الإعداد الأول  
**الناتج:** `Activated flutterfire_cli 1.3.1.`

---

### الخطوة 2: التحقق من Node و npm
```bash
node --version && npm --version
```
**ماذا يفعل:** يتحقق من تثبيت Node.js و npm  
**متى:** للتأكد قبل التثبيت التالي  
**الناتج المتوقع:**
```
v22.20.0
11.4.2
```

---

### الخطوة 3: تثبيت Firebase Tools
```bash
npm install -g firebase-tools
```
**ماذا يفعل:** يثبت أدوات Firebase على النظام  
**متى:** عند الإعداد الأول  
**المدة:** ~1 دقيقة  
**الناتج:** `added 750 packages in 53s`

---

### الخطوة 4: تحميل المكتبات
```bash
flutter pub get
```
**ماذا يفعل:** يحمل جميع الـ packages من pubspec.yaml  
**متى:** بعد إنشاء مشروع جديد أو تعديل pubspec.yaml  
**المدة:** 30 ثانية - 2 دقيقة  
**الناتج:** `Got dependencies!`

---

### الخطوة 5: فحص الكود
```bash
flutter analyze
```
**ماذا يفعل:** يفحص الكود عن الأخطاء والتحذيرات  
**متى:** بعد التحديثات الكبيرة  
**الناتج:**
```
✓ No errors found
ⓘ 5 info warnings (غير مهمة)
```

---

## أثناء التطوير

### تشغيل التطبيق
```bash
flutter run
```
**ماذا يفعل:** يبني ويشغل التطبيق على جهاز/emulator  
**متى:** أثناء التطوير  
**المدة:** 3-5 دقائق (المرة الأولى)

**خيارات:**
```bash
flutter run -v              # مع تفاصيل كاملة
flutter run -d device-id    # على جهاز محدد
```

---

### Hot Reload
في أثناء التشغيل، اضغط:
```
r      → Hot Reload (تحديث سريع)
R      → Hot Restart (تشغيل من جديد)
q      → الخروج
```

---

## أوامر مفيدة أخرى

### فحص الأجهزة المتاحة
```bash
flutter devices
```
**الناتج:**
```
Android SDK built for x86 • emulator-5554 • android-x86
Chrome (web)              • chrome       • web-javascript
```

---

### فحص شامل للإعداد
```bash
flutter doctor
```
**ماذا يفعل:** يفحص Flutter, SDK, IDE, Devices  
**الناتج:** علامات ✓ و ✗

---

### تنسيق الكود
```bash
dart format lib/
```
**ماذا يفعل:** ينسق الكود حسب معايير Dart  

---

### حذف الـ packages المتقادمة
```bash
flutter pub outdated
```
**ماذا يفعل:** يعرض packages فيها نسخ أحدث

---

---

# حل المشاكل

## المشكلة: "Target of URI doesn't exist"
```bash
flutter pub get
```

---

## المشكلة: "gradle build failed"
```bash
flutter clean
flutter pub get
```

---

## المشكلة: أخطاء غريبة
```bash
flutter clean
flutter pub get
flutter analyze
flutter run -v
```

---

## المشكلة: "permission denied" (Windows)
```bash
# أعد تشغيل Terminal كـ Administrator
```

---

## المشكلة: "flutterfire command not found"
```bash
dart pub global activate flutterfire_cli
```

---

## المشكلة: "firebase not found"
```bash
npm install -g firebase-tools
```

---

---

# البناء والنشر

## بناء APK (Android)
```bash
flutter build apk --release
```
**الملف النهائي:** `build/app/outputs/flutter-apk/app-release.apk`

---

## بناء IPA (iOS)
```bash
flutter build ios --release
```
**الملف النهائي:** `build/ios/ipa/Runner.ipa`

---

## بناء Web
```bash
flutter build web
```
**الملف النهائي:** `build/web/`

---

## بناء Windows
```bash
flutter build windows --release
```

---

## بناء Linux
```bash
flutter build linux --release
```

---

---

# الأوامر بـ Copy-Paste

## الإعداد الأول (كل في سطر)
```bash
dart pub global activate flutterfire_cli
```

```bash
npm install -g firebase-tools
```

```bash
flutter pub get
```

```bash
flutter analyze
```

---

## الإعداد الأول (أمر واحد)
```bash
dart pub global activate flutterfire_cli && npm install -g firebase-tools && flutter pub get && flutter analyze
```

---

## تشغيل التطبيق
```bash
flutter run
```

---

## تشغيل مع التفاصيل
```bash
flutter run -v
```

---

## عند مشكلة
```bash
flutter clean && flutter pub get && flutter analyze && flutter run
```

---

## البناء النهائي
```bash
flutter clean && flutter build apk --release
```

---

---

# 📋 جدول سريع

| الحالة | الأمر | المدة |
|--------|------|------|
| 🆕 إعداد جديد | `dart pub global activate flutterfire_cli` | 30 ثانية |
| 🆕 إعداد جديد | `npm install -g firebase-tools` | 1 دقيقة |
| 📥 تحديث packages | `flutter pub get` | 30 ثانية |
| 🧹 تنظيف | `flutter clean` | 10 ثواني |
| ▶️ تشغيل | `flutter run` | 3-5 دقائق |
| 🔍 فحص أخطاء | `flutter analyze` | 10 ثواني |
| 🏥 فحص الإعداد | `flutter doctor` | 30 ثانية |
| 🏗️ بناء APK | `flutter build apk` | 5-10 دقائق |
| 🏗️ بناء IPA | `flutter build ios` | 10-15 دقيقة |
| 🆘 مشاكل عامة | `flutter clean && flutter pub get` | 1 دقيقة |

---

---

# 🎯 الخطة الموصى بها

## اليوم الأول (الإعداد):
```bash
# 1. تثبيت الأدوات
dart pub global activate flutterfire_cli
npm install -g firebase-tools

# 2. تحميل المكتبات
flutter pub get

# 3. فحص الإعداد
flutter doctor
flutter analyze

# 4. أول تشغيل
flutter run
```

---

## التطوير اليومي:
```bash
# قبل البدء:
flutter pub get

# أثناء العمل:
flutter run

# عند التعديل:
r   # Hot Reload
```

---

## قبل النشر:
```bash
# تنظيف وبناء كامل
flutter clean
flutter pub get
flutter analyze
flutter build apk --release
```

---

---

# 💡 نصائح ذهبية

1. **احفظ هذا الملف:** ستحتاجه دائماً

2. **الترتيب مهم:** لا تقفز الخطوات

3. **قراءة الأخطاء:** الحل في رسالة الخطأ عادة

4. **استخدم -v:** عند الحاجة للتفاصيل

5. **الإنترنت مطلوب:** لتحميل الـ packages و Firebase

6. **الصبر:** كل خطوة تأخد وقتها

---

---

# 🔗 الملفات الأخرى للمراجعة

- `QUICK_COMMANDS.md` - أوامر سريعة جداً
- `COMMANDS_GUIDE_AR.md` - شرح شامل لكل أمر
- `PROJECT_COMMANDS.md` - الأوامر المستخدمة في هذا المشروع
- `VISUAL_COMMANDS.md` - شرح مرئي مع الرسوميات

---

**حفظ سعيد!** 📌
